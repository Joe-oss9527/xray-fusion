# CONTRIBUTING

- Run `make lint` and `make test` before pushing.
- Keep PRs scoped (single layer/contract per PR).
- Prefer POSIX-ish shell with Bash features; avoid `eval` and complex regex.
- Add/extend Bats tests for any new module or command.
